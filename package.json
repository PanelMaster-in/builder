{
  "name": "flyenv-php-builder",
  "private": true,
  "type": "module",
  "scripts": {
    "discover": "node scripts/discover-php.js",
    "push": "git add . && git commit -m 'Update PHP versions' && git push"
  }
}
